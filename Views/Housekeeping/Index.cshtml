@{
    ViewData["Title"] = "Trạng thái dọn phòng";
}

<div class="d-flex justify-content-end mb-3">
    <a class="btn btn-primary" asp-action="Requests">Yêu cầu phục vụ</a>
</div>

<partial name="_Placeholder" model="@("Bảng trạng thái dọn dẹp của tất cả các phòng (bẩn, đang dọn, sạch, đã kiểm tra).")" />
