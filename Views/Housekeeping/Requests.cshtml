@{
    ViewData["Title"] = "Yêu cầu phục vụ";
}

<div class="d-flex justify-content-end mb-3">
    <a class="btn btn-primary" asp-action="CreateRequest">Ghi nhận yêu cầu</a>
</div>

<partial name="_Placeholder" model="@("Danh sách yêu cầu dọn phòng / thêm đồ chưa xử lý.")" />
