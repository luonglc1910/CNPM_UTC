@{
    ViewData["Title"] = "Phòng";
}

<div class="d-flex justify-content-end mb-3">
    <a class="btn btn-primary" asp-action="Create">Thêm phòng</a>
</div>

<partial name="_Placeholder" model="@("Danh sách phòng theo tầng, loại phòng và trạng thái (sẵn sàng / bảo trì).")" />
